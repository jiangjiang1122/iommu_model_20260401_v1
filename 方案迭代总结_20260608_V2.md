# 方案迭代总结 - 20260608_V2

## 迭代内容

### 1. ✅ 配置参数统一定义 (新增章节 一.5)

**新增文件**: `iommu/include/iommu_dedup_params.hh`

统一定义了所有配置参数,包括:

| 参数类别 | 参数名 | 值 | 说明 |
|---------|--------|-----|------|
| **Buffer配置** | `DEDUP_BUFFER_CAPACITY` | 256 | Buffer容量 |
| | `DEDUP_BUFFER_BACKPRESSURE` | true | 反压策略 |
| **预取配置** | `DEFAULT_PREFETCH_DEPTH` | 8 | 默认预取深度D |
| | `PREFETCH_ENABLED_BY_DEFAULT` | true | 预取默认启用 |
| | `MAX_BURST_PTE_COUNT` | 64 | Burst最大PTE数 |
| **PTE标记** | `PTEStatus` | enum | PTE_VALID/INVALID/ERROR |
| **页大小** | `PAGE_SIZE_4KB` | 0x1000 | 4KB页大小 |
| **Sv39参数** | `SV39_VPN_BITS` | 9 | VPN位数 |
| | `SV39_PPN_BITS` | 26 | PPN位数 |
| | `PTE_SIZE` | 8 | PTE大小(字节) |

**使用方式**:
```cpp
#include "iommu_dedup_params.hh"

if (prefetch_depth == 0) { /* 预取关闭 */ }
if (buffer.valid_count >= DEDUP_BUFFER_CAPACITY) { /* 反压 */ }
```

---

### 2. ✅ DC Cache不修改 (新增章节 一.4)

**明确说明**:
- DC Cache (Device Context Cache) **保持现有实现**
- **不添加**预取和去重逻辑
- 仅PT Cache实现去重+预取机制
- DC Cache不参与占位CL、Buffer链表、Burst预取等优化

**不修改文件清单**:
- `iommu/cache_src/cache/dc_cache.cpp`
- `iommu/cache_src/cache/dc_cache.hh`
- `iommu/cache_src/common/dc_types.h`

---

### 3. ✅ PTW预取返回PTE标记机制 (新增章节 2.4 + 第六章更新)

#### 3.1 核心要求

**PTW预取返回时,必须返回1+D笔响应,每笔响应必须携带PTE状态标记**:

```cpp
enum class PTEStatus : uint8_t {
    PTE_VALID = 0,          // 正常有效PTE (当前统一使用)
    PTE_INVALID = 1,        // 无效PTE (V=0)
    PTE_ERROR = 2,          // 错误PTE (权限错误等)
    PTE_RESERVED = 3        // 保留
};
```

#### 3.2 数据结构更新

**新增 `pte_update_t` 结构**:
```cpp
struct pte_update_t {
    uint64_t iova;           // 4KB对齐的IOVA
    uint64_t ppn;            // 物理页号
    pte_t pte;               // 完整PTE
    PTEStatus status;        // PTE状态标记 [新增]
};
```

**更新 `ptw_response_t` 结构**:
```cpp
struct ptw_response_t {
    uint32_t task_id;
    std::vector<pte_update_t> pt_updates;  // D+1个结果
    bool is_main_task;
    bool prefetch_enabled;
    uint32_t prefetch_depth;
    uint64_t main_iova;
    uint64_t actual_page_size;
};
```

#### 3.3 实现策略

**当前阶段 (V2.0)**:
- ✅ 每笔响应**必须携带** `status` 字段
- ✅ **统一标记**为 `PTEStatus::PTE_VALID`
- ❌ **暂不处理** 错误/无效PTE

**未来扩展 (预留)**:
```cpp
// TODO: 未来可添加PTE状态检查
if (!pte.valid) {
    update.status = PTEStatus::PTE_INVALID;
} else if (pte.permission_error) {
    update.status = PTEStatus::PTE_ERROR;
}
```

#### 3.4 代码实现位置

- **数据结构定义**: `iommu/include/iommu_req_rsp.hh`
- **Burst响应处理**: `iommu/iommu_perf_model/iommu_perf_ptw.cc`
  ```cpp
  // 主任务结果
  main_update.status = PTEStatus::PTE_VALID;
  
  // 预取结果 (D个)
  for (uint32_t i = 0; i < prefetch_depth; i++) {
      update.status = PTEStatus::PTE_VALID;  // 暂统一标记
  }
  ```

---

## 文档更新位置

完整方案文档: `PT_CACHE_DEDUP_PREFETCH_INTEGRATED_V2_20260608.md`

主要修改章节:
1. **一.4 DC Cache说明** (新增) - L27-33
2. **一.5 配置参数统一定义** (新增) - L35-160
3. **2.4 PTW响应数据结构** (新增) - L270-345
4. **第六章 刷新流程** (更新) - L523-530 (PTE标记说明)
5. **第五章 代码实现** (更新) - L577-610 (PTE标记实现)
6. **第九章 修改文件清单** (更新) - L1144-1175 (添加参数文件、DC Cache说明)

---

## 关键设计决策

### 决策1: Buffer容量=256
- **原因**: 支持256个并发任务链表
- **反压策略**: Buffer满时阻塞等待
- **配置位置**: `iommu_dedup_params.hh`

### 决策2: DC Cache不修改
- **原因**: DC Cache已有成熟实现,不需要预取优化
- **影响范围**: 仅PT Cache参与优化
- **维护成本**: 低 (不修改现有DC代码)

### 决策3: PTE标记预留
- **原因**: 简化当前实现,预留未来扩展
- **当前策略**: 统一标记为PTE_VALID
- **未来扩展**: 可快速添加错误处理逻辑
- **风险控制**: 预留enum和status字段,避免后续大改

---

## 验证检查清单

- [x] Buffer容量参数=256,统一定义到头文件
- [x] DC Cache相关文件不修改
- [x] PTW响应必须返回1+D笔
- [x] 每笔响应携带PTEStatus标记
- [x] 暂统一标记为PTE_VALID
- [x] 预留错误/无效PTE处理接口 (TODO注释)
- [x] 参数头文件包含所有关键参数
- [x] 代码示例展示参数使用方式

---

## 下一步

1. **评审本方案文档**
2. 评审通过后开始代码实施 (Phase 1-6)
3. 创建 `iommu_dedup_params.hh` 参数头文件
4. 实现PTE标记机制
5. 验证DC Cache未被修改

---

**文档版本**: V2.0  
**更新日期**: 2026-06-08  
**作者**: AI Assistant
