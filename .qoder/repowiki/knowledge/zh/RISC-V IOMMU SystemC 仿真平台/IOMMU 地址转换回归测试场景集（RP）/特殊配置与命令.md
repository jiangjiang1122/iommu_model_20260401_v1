每个测试场景在 `send_translation_request_1_thread()` 中自包含初始化流程：先调用 `enable_iommu(iommu_ptr, DDT_1LVL)` 启用 IOMMU，再手动设置 `next_free_page`/`next_free_gpage` 避免页号冲突，随后通过 `add_device` 配置设备上下文与页表根，最后通过 `iodir`/`iotinval` 失效缓存后批量注入 TLM 请求并等待 `response_count_event` 完成验证。