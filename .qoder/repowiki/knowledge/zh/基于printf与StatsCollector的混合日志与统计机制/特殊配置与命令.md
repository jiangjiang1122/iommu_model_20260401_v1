- **环境要求**：必须在 WSL (Windows Subsystem for Linux) 环境下编译运行，依赖系统级安装的 SystemC 库。
- **场景化构建**：通过 `make TEST=<scenario>` 切换测试负载，如 `rand4k_singlestage`（随机读）、`seq128k_twostage`（顺序读+两阶段翻译）等。
- **专项测试**：提供 `test_dedup_unit` 和 `test_dedup_integration` 目标，用于独立验证 PT Cache 的去重与预取逻辑。