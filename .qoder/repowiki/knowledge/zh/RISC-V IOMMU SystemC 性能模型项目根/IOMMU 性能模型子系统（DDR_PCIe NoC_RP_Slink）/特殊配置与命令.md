RP 测试通过 `enable_cq`/`enable_fq`/`enable_disable_pq`/`enable_iommu` 依次初始化 CQ/FQ/PQ/DDT，再调用 `iotinval`/`ats_command`/`generic_any` 将命令写入 CQ 缓冲区并调用 `process_commands` 触发 IOMMU 处理；页表构建通过 `add_g_stage_pte`/`add_s_stage_pte`/`add_vs_stage_pte` 在虚拟 DDR 内存中逐层分配 PTE。