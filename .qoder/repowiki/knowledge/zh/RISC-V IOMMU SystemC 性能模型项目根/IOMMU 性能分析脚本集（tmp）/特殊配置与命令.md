多数脚本需先通过 build_asan_ptw18.sh 或 run_walker_regression.sh 等脚本执行 `make TEST=<scenario> SCENE7_PTW=<N>` 生成仿真二进制与 log，再运行对应 Python 分析脚本；log 路径通常硬编码为 `/mnt/d/Qoder_proj/iommu_model_20260401_v1/sim_*.log`，需在相同工作目录下执行。