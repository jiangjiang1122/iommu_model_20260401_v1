- Python 脚本统一通过 `re.search(r'\[TAG\].*?([\d.]+) ns', line)` 模式从仿真 log 中抽取时间戳与字段值，并以字典/列表聚合后计算稳态窗口统计。
- Shell 构建脚本固定采用 `cd /mnt/d/Qoder_proj/iommu_model_20260401_v1 && make clean && make TEST=... SCENE7_PTW=... CXXFLAGS='...'` 三段式结构，将调试宏与包含路径集中写入 CXXFLAGS。
- 统计分析脚本默认剔除前 5%~95% 数据作为稳态窗口（steady-state），并在输出中同时打印全窗口与稳态两种 IOPS 指标。
- 脚本间通过独立的命令行参数或硬编码路径传递输入 log 文件名，不引入模块间依赖，保持单文件可独立运行。