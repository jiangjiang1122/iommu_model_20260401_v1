必须在 WSL2 中执行；推荐使用 `./compile_wsl.sh` 或 `make TEST=<scenario>` 选择测试场景（默认 `rand4k_singlestage`，支持 `sv48_bare`、`seq128k_twostage`、`cache_inval`、`seq512b_2mb_twostage_s2on` 等）；调试模式 `DEBUG=1` 启用大量宏开关（`DEBUG_TRANSLATION`、`DEBUG_COMMANDS`、`DEBUG_FAULTS` 等）。