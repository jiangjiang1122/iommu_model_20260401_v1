通过 `iommu/cache_config/default_config.json` 配置缓存几何、失效参数与统计选项；仿真启动时需确保 `before_end_of_elaboration()` 中 `reset_iommu()` 成功返回；终止前需等待 `cache_sub.pt_update_fifo`、`pt_ram_fifo_pending()`、`dedup_ram_fifo_pending()` 排空以保证统计准确。