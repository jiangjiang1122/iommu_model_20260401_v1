- **WSL 强制要求**: 必须在 WSL2 Linux 环境下编译运行，依赖系统级安装的 SystemC 库。
- **场景化构建**: 通过 `make TEST=<scenario>` 选择测试负载（如 `rand4k_singlestage`, `seq128k_twostage`），自动链接对应的测试线程源文件。
- **专用测试目标**: 提供 `test_dedup_unit` 和 `test_dedup_integration` 用于验证 PT Cache 去重与预取逻辑。